{
  "name": "layerlane-apply",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "build": "npm run build --workspace=web",
    "dev": "npm run dev --workspace=web",
    "lint": "npm run lint --workspace=web"
  },
  "dependencies": {
    "next": "15.5.15",
    "react": "19.1.0",
    "react-dom": "19.1.0"
  },
  "engines": {
    "node": ">=20"
  }
}
