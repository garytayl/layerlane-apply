/** Shared form field styles (template-aligned tokens). */
export const fieldInputClass =
  "rounded border border-input bg-background px-2 py-1 text-sm text-foreground placeholder:text-muted-foreground focus:border-ring focus:outline-none focus:ring-2 focus:ring-ring/25";

/** Slightly larger controls (e.g. job paste form). */
export const fieldInputLgClass =
  "rounded-lg border border-input bg-background px-3 py-2 text-sm text-foreground placeholder:text-muted-foreground focus:border-ring focus:outline-none focus:ring-2 focus:ring-ring/25";

export const fieldCardClass =
  "rounded border border-border bg-card/40 p-4";

export const listItemCardClass = "rounded border border-border p-3";

export const primaryButtonClass =
  "w-fit rounded-md bg-primary px-3 py-1.5 text-sm font-medium text-primary-foreground transition hover:bg-primary/90";
