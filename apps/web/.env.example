# Supabase (Project Settings → API)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Server-only: service role for API-token-authenticated routes (never expose to client)
SUPABASE_SERVICE_ROLE_KEY=

# OpenAI (Analysis)
OPENAI_API_KEY=

# Public URL of this app (used by auth redirects in production)
NEXT_PUBLIC_APP_URL=http://localhost:3000
